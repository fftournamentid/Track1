- [expo-file-system v56 legacy import](expo-file-system-legacy.md) — v56 breaks documentDirectory; always import from expo-file-system/legacy in shareService
- [Truck Invoice Manager architecture](truck-invoice-arch.md) — InvoiceContext.duplicateInvoice needs two args (id + newNumber); generatePDF returns PDFResult not string
- [Ionicons icon availability](ionicons-icons.md) — "crown" is NOT in this Ionicons build; use Feather "award" for premium/medal UI instead.
- [Firebase Auth persistence](firebase-auth.md) — getAuth(app) uses browserLocalPersistence on web (Expo preview) — users stay logged in across browser close. No extra config needed for the web preview workflow.
- [Profile auto-save pattern](profile-autosave.md) — Uses userEditedRef + debounce timer to avoid auto-saving on Firestore-synced initial load; initializedRef prevents first Firestore→state sync from triggering the auto-save effect.
- [Template + TemplatePicker system](template-system.md) — TemplatePicker now uses onGenerate(templateId) callback; caller handles generation + sharing. Cache key = invoiceNumber + templateId.
- [Logout fix — no finally on success path](logout-fix.md) — after successful signOut, call router.replace then return; never use finally to reset state because the component unmounts causing a stale-state flash.
- [PDF Android fix — copy to stable URI](pdf-android-fix.md) — Print.printToFileAsync temp URI fails getContentUriAsync on Android; always copy to documentDirectory first, then fall back to Sharing.shareAsync if IntentLauncher throws.
- [Premium page — FOUNDERS EDITION design](premium-founders.md) — Premium page shows "Early Access Premium / Free for First 100K Users" with no payment options; all templates unlocked (isPremium=true for all); TemplatePicker shows FREE badges not PRO/locked.
- [Admin panel Firestore rules](admin-firestore-rules.md) — Firestore client-side rules block admin from reading all users; firestore.rules at project root has isAdmin() helper; deploy via Firebase CLI or Console before admin panel can load users.
- [Admin routing — never redirect in login.tsx](admin-login-routing.md) — login.tsx must NOT call router.replace after signIn; let _layout.tsx handle routing after userDoc loads. Otherwise admin lands in /(tabs) instead of /admin because userDoc hasn't arrived yet.
- [Admin UID auto-promotion](admin-uid-promotion.md) — AuthContext auto-sets role:"admin" in Firestore for UID kaqcXOcHHYU7VeSXdLMUR2E66vB3 on first login using setUserRole(); no manual Firestore Console step needed.
- [PDF flow architecture](pdf-flow-arch.md) — generate → save to documentDirectory → update Firestore (pdfUrl/pdfName/pdfCreatedAt) → show PDFActionModal. Cache file by invoiceNumber+templateId; forceRegenerate bypasses cache. Firestore undefined → use deleteField(). updateInvoiceDoc now auto-converts undefined values to deleteField().
- [Admin panel — single file with internal tab state](admin-panel-tabs.md) — admin/index.tsx uses internal Tab type ('dashboard'|'users'|'invoices'|'premium'|'analytics'|'more') with 6-tab custom bottom bar; announcements navigates via router.push('/admin/announcements').
- [PDF size verification](pdf-size-verification.md) — generatePDFWithTemplate verifies size > 1KB via FileSystem.getInfoAsync after printToFileAsync; retries once on failure; throws if still too small. FileSystem must be imported from expo-file-system/legacy.
- [Announcement system](announcement-system.md) — /announcements Firestore collection; subscribeToActiveAnnouncements for home screen banners; full CRUD in admin/announcements.tsx; rules: auth users read, admin writes.
